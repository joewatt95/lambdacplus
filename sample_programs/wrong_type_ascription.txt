// Type ascriptions in function bodies cause trouble.
// This occurs because we try to unparse the input expression and we do so
// using incorrect indices. 
// This occurs because the problematic variable may occur under a binder
// and this causes problems if we directly look it up in the naming context.
// 
// We should instead just grab the user's input from the source code using the
// source locations.

def test := fun (T : Type) => (T : Kind)

def test := fun (T : Type) (x : T) => (x : Kind)